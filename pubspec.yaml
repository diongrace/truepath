name: truepath
description: "biblique."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.0 # Assurez-vous que c'est au bon niveau
  # confetti: ^0.6.0  # Décommentez si nécessaire
  cupertino_icons: ^1.0.6
  http: ^1.2.2
  carousel_slider: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
